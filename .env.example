# 由部署环境注入，Node 不自动读取 .env；不要提交真实密钥。
WECHAT_APP_ID=
WECHAT_APP_SECRET=
NODE_ENV=production
HOST=127.0.0.1
PORT=8787
DB_PATH=data/shadowtable.sqlite
# 仅本地测试可设1，生产必须禁用。
DEV_AUTH=0

# 本地陪测面板，生产始终不可用。
DEV_PANEL=0
