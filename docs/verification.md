# 验证记录

2026-09-14，Node v22.23.2，运行 `npm test` 和 `npm run check`，当前32项测试通过。

## 已自动验证

- 6–10人基础角色数量、梅林唯一、随机首队长合法、个人视图；10/12完整角色各自视野（含双奥伯伦）及完整终局。
- 11人逆仆视野、成功票限制、先刀逆仆/再刺梅林的四种命中组合、转阵营及个人胜负、重开清理。
- 影中执刃辅助模式身份组合、奥伯伦/红内奸强制失败、蓝内奸成功、线上任务到线下结算再重开，不冒充自动胜方。
- 座位冲突、准备条件、跨房拒绝、非房主越权、陈旧阶段请求。
- 未确认板子不能创建；秘密提交不改变房主/其他玩家公共响应。
- 组队平票否决、连续5次否决坏人胜；组队票结算前保密、结算后按座位公开。
- 好人不能投失败，非队员不能交任务票，重复提交/目标/非法队长被拒绝。
- 好人三成功+刺杀失误、刺杀命中、三任务失败、第4轮双失败阈值。
- 结束/终止不公开全员角色，同房重开清除旧状态重新准备和洗牌。
- HTTP并发准备、相同编号重复开局、创建幂等、SQLite重启后的会话/房间/回执恢复。
- 微信登录服务边界用可控换码适配器验证：用户身份由服务器返回OpenID决定，忽略客户端伪造OpenID；禁用开发登录时无法调用。
- 6人基础、11人逆仆、12人影中执刃辅助：多个小程序页面控制器（VM运行真实页面JS，微信网络适配为真实HTTP请求）贯通创建→加入→准备→身份→组队→表决→任务→最终结算→重开。
- 身份请求在切后台后返回不重新揭牌；过期公共响应不覆盖新阶段；确认弹窗跨阶段不误操作；网络丢失响应保留同一请求编号，不提前显示成功；应用重启恢复未确认建房且不缓存秘密操作。

## 模板与布局验证

- WXML/WXSS编译通过；入口按钮busy状态、加载/错误条件、非房主管理按钮、身份遮盖、提交后禁用操作均在编译树测试中验证。
- 使用Playwright查看375px竖屏和844px横屏的编译树HTML布局投影（脚本 `scripts/render-layout.js`，本地产物 `output/playwright/`）。这是静态布局QA，不是另一个游戏客户端，也不能替代微信原生渲染。375px检查未出现横向溢出，主要可操作按钮高度48px。
- 将私密面板移到座位列表前，避免12人时主要操作落到长座位列表之后。

## 尚未实机验证

已安装官方微信开发者工具ARM64 2.02.2608060，但导入项目时工具返回「Error: 需要重新登录」，尚未进入原生编译。已使用 miniprogram-compiler 0.2.3（微信 wcc/wcsc 编译器的 Node 封装）验证模板和样式，并检查编译树的遮盖/条件/禁用状态。未验证最新开发者工具编译、大字体布局、读屏及真实微信请求。未提供真实AppID、AppSecret、合法HTTPS域名，微信换码链路的网络端点未使用真实凭证验证，未上传微信审核/发布。

交付前的真机验收步骤：

1. 按README配置真实微信应用与HTTPS域名，导入项目，先检查WXML/WXSS编译通过。
2. 六台独立手机加入同房，检查375px宽屏、横屏、安全区和最大系统字号；按钮至少44px触控，长昵称不挡座位号码。
3. 房主分别扮演好人/坏人，确认其网络返回只含本人的秘密；从其他人的会话访问房间应403。
4. 所有人主动揭牌；切后台、锁屏、从分享页进入后身份必须遮盖；人为延迟请求验证旧响应不会揭牌。
5. 完成一轮组队赞成/反对，确认提交前后其他手机不显示人数/谁已提交；结算后才出现公开组队票。
6. 中断网络后提交，再恢复网络重试；只执行一次，服务器未确认前不能出现成功。关闭/重启服务，成员和已交动作恢复。
7. 完成三失败、三成功刺错、三成功刺中、第4轮一失败仍成功的场景；结束同房重开，所有人重新准备。
8. 检查已禁用板子在列表明确显示待确认，直接请求接口也不能绕过。

以上原生/真机项目保持未验收，不用控制器测试替代实际UI测试。

新增恢复验证：本人可列出自己创建及加入的多个房间；外人列表为空；无登录返回401；离开后成员关系移除；返回字段白名单不包含秘密状态；邀请入口不被本地旧房间覆盖。

## 最新原生观察

开发者工具现已打开项目并渲染原生首页，登录阻塞解除；仍有网络请求失败，尚未完成原生联网对局与真机验收。本轮细化客户端网络错误提示，五种注入失败均保持拒绝，不产生成功回执；API脚本语法检查通过。原有32项完整测试记录不代表新增原生验收通过。

## 原生联网进展

通过本项目本地联调设置解除域名校验阻塞后，原生客户端建房187443获得「服务器已确认」，五个独立HTTP测试账号加入并准备，原生房主也准备成功。点击开始后显示微信原生确认弹窗。自动化工具对弹窗坐标点击返回noWindowsAvailable，未声称已经开局。上述证明原生请求/回执/同房状态刷新，不能替代完整原生对局及真机验收。修正了两个禁用板子的过时说明：任务人数已确认，不再把任务配置列为待确认项。

## 原生任务结算实测

本轮开始确认按钮恢复可访问后，原生房主完成开局、本人身份查看与确认（提交后自动遮盖）、推进到组队、赞成票提交、全票通过结算、秘密任务牌提交和任务结算。五名伙伴使用独立HTTP账号，未从服务端全态代替玩家决策。页面显示第一轮一张失败、任务失败，未披露出牌人。终止弹窗已显示但确认控件暂不可访问，尚未完成原生终止/重开。当前项目AppID已从touristappid变为已配置值，仍使用本地开发登录，不能据此宣称正式微信登录通过。

本轮npm test：32通过、0失败。上述原生证据只覆盖一个经典6人局的一轮任务，不覆盖所有板子、真机后台遮盖或正式部署。

## 原生终止与重开、座位可读性

原生确认终止后显示「不判胜负」，同房再开恢复准备阶段：房间码187443与六个座位保留、身份卡及旧任务记录消失、六人准备状态清零。随后六人重新准备。第二局开始确认仍待完成，未把回到准备页称为第二局已开局。

修正微信原生disabled按钮默认底色覆盖座位样式的问题。原生截图显示深色座位底、清晰昵称/准备文字，任务座位保留独立底色规则。模板与WXSS编译3项检查通过；前轮完整32项通过记录仍有效。

## 同房第二局已开局

房间187443通过原生确认再次开局，显示第2局/任务1、队长6号、私密身份确认、默认遮盖身份卡。旧任务记录不再显示。此证据补齐原生房主的一轮任务→终止→同房再次开局；五名伙伴为独立HTTP账号，不称为六台真机验收。

## 2026-09-14 入口与交互简化

- 中文品牌、导航栏、分享标题统一为「桌边助手」。首页移除大段介绍，创建/加入分开；人数优先，可用板子随人数过滤，单板自动选择。规则与房主设置按需展开；准备/开局移至座位列表前。
- 昵称使用微信 nickname 输入框和原生 form submit 最终值。官方文档已核对，成功进房后缓存；不是静默获取微信资料。
- 最新完整测试34项通过，语法检查通过；新增人数切换、12人板子选择、房内兼容配置及昵称最终值/清空/缓存验证。
- 微信开发者工具原生观察：6人自动选择、12人两板切换、加入入口、原生表单建房654568（昵称正确）、准备操作前置。修正原生form默认显示与按钮默认宽度覆盖，6/12人截图布局正常。测试房654568保留，当前工具返回首页。
- 编译模板5场景（6人入口、12人入口、加入、12人准备、身份）在375px和844px检查未见横向溢出。静态模板投影不代替真机；微信昵称候选、安全检查、最大系统字体与读屏仍需真机验收。

## 2026-09-14 默认加入、牌桌删除与离开恢复修复

移除品牌旁标语，首页默认加入。当前房主可在列表删除牌桌，服务端检查权限和阶段；删除与幂等回执在同一事务提交。原生确认弹窗已验证并取消，未删除已有用户牌桌；接口与双客户端删除使用独立内存数据库验证。

修复离开后列表未刷新，以及失效房间持续轮询。房间403/404清除缓存、身份和轮询；返回入口后延迟错误不会影响新界面。最新39项测试通过、语法检查通过。原生首页正常显示加入入口和房主删除按钮，本地后端已重启加载新增接口。

## 2026-09-14 10人规则更正与错误弹窗

10人仅保留用户确认的经典基础角色配置；新增测试验证旧配置被拒绝、10人自动选唯一板子。错误改居中弹窗，验证业务错误关闭保持房间、恢复错误重试与未确认请求保护。42项测试通过、语法检查通过。微信原生预览已观察输入错误弹窗及关闭返回原页面，按钮与文字分行；后端已重启。

## 2026-09-14 昵称、座位与连接状态

修正原生输入框上下padding造成的昵称偏下，以及微信非mini按钮默认宽度覆盖座位网格导致的重叠。自己的座位增加金色边框、底色和「你」标签；离开按钮改为明显的红色描边按钮，执行前确认，取消或确认期间切房不会退出。原生11人座位截图已确认分列间距、本人高亮及离开确认/取消正常。

切换牌桌移至品牌栏右侧并保留座位；离开才释放成员关系。服务器确认文字替换为连接状态点：最近请求成功绿色，操作等待黄色，断网/连接未确认红色，可点击查看说明。44项自动测试及语法检查通过。

## 2026-09-14 空桌生命周期

离开不再触发删除，列表同时按当前房主归属和成员关系查询；离席房主显示重新入座。新增房主管理摘要仅返回公共元数据，不开放离席私密视图。删除仍校验当前房主、阶段与幂等编号。46项测试通过：包括空桌跨SQLite重启保留、离席房主再入座、他人不能接管或管理、离席房主明确删除、客户端离开后列表更新与再入座。

## 本地陪测面板

新增server/dev-panel静态控制台，通过普通开发登录、成员public/private接口、带幂等编号的玩家命令控制独立测试账号。未添加特权状态接口；当前小程序玩家不会被批量操作。

本次全套54项测试通过，语法检查通过。新增陪测专用测试覆盖显式开关、生产环境误配置关闭开发登录/面板、外部Host/Origin/非本机拒绝、真实HTTP全局任务/刺杀/重开、未确认回执跨刷新原编号重试。浏览器使用独立内存数据库房间，验证添加/补齐五名玩家、批量准备、刷新恢复、身份查看和批量确认，桌面及375px无横向溢出。部署面板资源不包含在小程序包中。


## 按需桌游工具（2026-09-14）

小程序发牌默认进入自由操作；新增房主投票、任务、刀梅林、刀逆仆、作废和结束操作，旧房间可无损切入。HTTP客户端测试覆盖直接任务→独立投票→刀梅林→房主结束；引擎覆盖连续操作不自动结束、参与者提交约束、秘密票隔离、逆仆私密结果、线下刀人、旧请求拒绝、管理权限和同房重开。70项测试通过，语法及WXML/WXSS编译检查通过。375px编译页面布局检查覆盖房主面板和任务选人弹窗；未以HTML投影代替微信真机交互验收。


## 房主完成进度与紧凑身份入口（2026-09-14）

按用户最新要求，房主可查看当前操作的逐玩家完成状态及总计，不获取票型、目标或角色；普通玩家不获取此明细。任务区分未入选玩家，刀人阶段所有玩家都有操作／确认要求，避免通过参与者列表反推刺客。身份默认折叠为一行；结束本局按钮移至公开记录后的页面底部。73项测试通过，包括不同秘密票产生相同房主状态、权限隔离、刀人确认和模板布局；375px布局投影已检查。

## 2026-09-14 扩展板操作验证

- `npm test`：95 项通过，包括十二骑士手机页面经 HTTP 发牌、同时秘密提交技能、结算、换轮，以及混沌契约四类票权限和汇总。
- 技能规则覆盖：分层B牌、守护免死、女巫替死/自替死、换号与未触发不消耗、猎人链与死亡取消待执行动作、圣骑士保留技能状态、复活甲、B牌耗尽、兰斯洛特转换、仙女/先知私密视野、最终空刀、重开及作废恢复。
- 隐私覆盖：房主仅查看提交进度、不返回B牌/隐藏防护；转红不新增同伴视野；选择盘刀人不会通过失败响应探测隐藏阵营。
- `npm run check`、`git diff --check` 通过。
- Playwright 检查了编译 WXML 的 375px 布局投影：十二骑士房主按钮、技能长列表滚动和关闭入口。此检查不替代微信真机验收。

## 操作精简与新身份提醒验证

- 103项自动化测试通过，新增技能确认取消/过期保护、复活身份版本与本人确认、旧房间换牌提醒、夜晚自动更新、移除按钮与新身份模板验证。
- 用隔离的内存数据库在真实浏览器验证陪测玩家的新身份弹窗、当前牌面和确认回执；不修改现有牌桌数据。
- 本地8787服务已重启，接口已返回简化后的板子名称。

## 自动轮次推进验证

105项测试通过；覆盖新B身份下一轮技能资格、任务自动推进、投票不推进、非法/过期请求不修改轮次、作废后重开不重复推进、第五轮上限，以及编译模板中移除手动换轮入口。语法检查与diff空白检查通过。

连续技能修复：108项测试通过，新增直接再次发起技能、B技能生效、取消重试不重复转换、任务/手动转换后不重复推进，以及第五轮越界不改状态的验证。

## 多人陪测限流修复

111项测试通过。新增同IP十二账号720次正常轮询不互相限流、单账号180次/分钟限流仍生效、429返回Retry-After且窗口到期恢复、陪测同阶段私密视图复用、客户端冷却期不请求且显式重试保留幂等编号的验证。

同IP总额度调整为6000次/分钟，单账号180次/分钟、登录30次/分钟、创建8次/分钟保持不变。陪测每4秒仍更新公开状态，但仅阶段变化时重新读取私密视图。429后两端请求遵守冷却时间，小程序普通轮询自动恢复，未确认提交仍需原编号显式重试。

## 技能过程与最终结果公示

115项测试通过。新增默认过滤过程、房主设置权限与切换、旧日志过滤、守护/换号/B耗尽的最终汇总、猎人链中间状态不公示，以及房主开关模板验证。最终汇总不含角色牌面。

独立设置页：121项测试通过，覆盖非管理员访问、草稿与统一保存、保存重试编号、阶段冲突刷新、服务端权限/原子校验、独立页面编译和原生开关。检查375px布局投影（原生开关仅近似绘制），微信真机效果仍以开发者工具/真机为准。

- 辅助流程优化：124 项测试通过。新增首轮连续仙女查验且仅持有者提交、独立连续阵营转换及牌堆耗尽原子校验、三个辅助板子仅队员参与任务；更新五轮后继续操作与技能不自动抽转换牌的回归测试。

## 自动结算与结束等待（2026-09-18）

- `npm test`：176 项通过；覆盖按需模式投票、任务、仙女、技能与猎人链自动结算，房主截止权限／确认／旧阶段保护，弃权门槛、无参与者兜底、任务与必选目标作废、私密结果保护、HTTP 并发与幂等重试、SQLite 重新打开后的提交及结果恢复。
- `npm run check` 与 `git diff --check` 通过。小程序 WXML/WXSS 编译及页面控制器测试通过；已更新原手动结算测试以验证最后提交直接产生结果，历史顺序模式回归保持通过。
- Playwright 实际浏览器检查：375×812 的等待、截止确认、结果卡与下一项保留结果；812×375 横屏；150% 缩放并开启减少动态效果时无横向溢出，结束等待按钮可见。浏览器控制台无错误或警告。
- 本次未做微信真机验收，未部署或上传小程序。

## 房间设置移出玩家（2026-09-18）

- `npm test`：192 项通过；新增房主权限、仅准备／结束／终止阶段允许移出、不能移出本人、旧座位与重新入房身份校验、HTTP 幂等重试、移出后权限撤销、重新加入、历史结果保留，以及两端确认取消、草稿保留、失败重试与提示验证。
- `npm run check` 与 `git diff --check` 通过，小程序设置模板编译通过。
- Playwright 在独立内存数据库中检查 375×812 的长昵称、11 人选择列表和移出确认弹窗；实际移出后，玩家收到“你已被房主移出房间”并返回加入页面，重新打开旧房间也显示相同提示。浏览器控制台无错误或警告。
- 未做微信真机验收，未部署；未重启已有本地服务。

## 圣骑士、猎人和魔术师验收修复（2026-09-19）

- `npm test`：216 项通过。新增圣骑士出局后选人、优先于猎人、救回取消枪、跳过后再次询问、不能自救／救女巫替死者、全员掩护和私密目标、主动自爆与被动共享次数、复活不重置猎人技能、作废还原、追加阶段截止与 SQLite 重启恢复。
- 保留并增加回归：五类 A 刀客／刺客互刀组合及下轮次数、循环刀、单次守护挡不住两刀、梅林／派西复活甲后再次抽 B、仙女莫德雷德及转换后双兰斯阵营、仙女传递、先知视野；投票、任务、转换等既有流程全部通过。
- 魔术师验证骑士因映射判错自爆、刀错、守护和女巫替死映射均正常耗次；删除连带死亡，无攻击结算不消耗。
- Playwright 在独立内存牌局检查陪测面板双号码选择、限制两人、二次确认及提交成功；网页版圣骑士展示当轮出局号码和二次确认。检查 375×812 / 812×375 布局，以及放大字体和减少动态效果，无横向溢出。小程序页面控制器与模板编译测试通过。
- 未做微信真机验收，未部署；未操作已有牌局或重启已有本地服务。

## 管理操作记录简化（2026-09-19）

- 行动前保存玩家及参与者身份快照，历史记录不随抽 B 牌变化；旧日志缺失身份时明确标注“身份未记录”。新增操作保存发起时间，技能及其圣骑士／猎人追加阶段使用同一时间；旧日志显示最早记录时间，不冒充发起时间。
- 操作记录移除明细展开及局／轮标题，保留按阶段、座位汇总和房间分页。仅管理员接口读取身份快照。
- 219 项测试、语法检查及 diff 检查通过；Playwright 独立内存牌局验证姓名／身份／行动和发起时间正确呈现，明细展开数量为零。未部署。
