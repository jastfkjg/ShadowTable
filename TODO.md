# TODO

多人使用场景下识别出的潜在问题与后续改进。按优先级排序，均已确认现象与影响面，改动前请结合 [架构文档](docs/architecture.md) 与 [部署文档](docs/deployment.md) 评估。

实测结论（2026-09-19，单进程 HTTP 栈 + 内存 SQLite）：90 名玩家按客户端 2.5s 轮询节奏时 p95 延迟 13ms、0 错误；9 人并发提交写延迟 3–6ms；12 人骑士局状态（11.6KB）构造一次视图约 4µs。当前架构对「几桌线下聚会」规模无性能问题，真正的风险在限流上限与数据不清理。

## 高优先级

### 1. 生产环境全站共享单个 IP 的限流额度（最可能卡住的场景）

- **现象**：部署经 Caddy 反向代理后，所有玩家的连接均来自同一 IP，而限流按连接 IP 计算（`server/app.js` 中 `limit(ip:..., 6000)` 与 `limit(login:..., 30)`）；部署文档 [deployment.md:89](docs/deployment.md#L89) 已明确「代理后玩家共享代理 IP 额度（登录30次/分钟，总请求6000次/分钟）……扩大规模前需实现可信代理 IP 处理」。
- **影响**：客户端每 2.5s 轮询一次公共视图（`miniprogram/pages/table/table.js`、`server/web/app.js`），即每人约 24 次/分钟；全站以单 IP 计约在 **250 名玩家同时在线**时触顶 6000 次/分钟，之后轮询大量返回 429，前端进入「冷却后自动刷新」退避，表现为整体刷新卡住。首次登录突发（如聚会开始时一桌 12 人同时登录）也会撞上登录 30 次/分钟的上限。
- **建议**：
  - 在可信反向代理后按 `X-Forwarded-For` 使用真实客户端 IP 限流（Trusted proxy 白名单），而不是连接 IP；
  - 提高或拆分的共享额度，避免单 IP 成为全局天花板；
  - 为 `/health` 增加请求延迟与 429 计数监控，接近额度时告警。

### 2. 房间与回执数据无自动清理，长期运行持续变慢

- **现象**：MVP 阶段不自动清理房间、会话、幂等回执与管理审计（架构文档 [architecture.md:36](docs/architecture.md#L36) 已注明为已知取舍）。体现在代码上：
  - 「我的牌桌」`GET /api/me/rooms` 通过 `json_each` 扫描**全部房间**并解析每间房的 JSON（`server/store.js` `roomsFor`）；
  - 建房时的 6 位随机码防碰撞探测 `do { ... } while (store.get(code))` 随房间总量线性变慢（`server/app.js`）；
  - `sessions` 只在查询时按 `expires` 过滤、行不删除；`receipts`（每次写操作 1 行）与 `admin_audit` 无限增长；WAL 文件随写入持续膨胀（当前库主文件 651KB 时 WAL 达 1.57MB）。
- **影响**：单次聚会与短周期运行无影响；连续开服数月后，房间列表接口与建房操作延迟显著上升，数据库体积不受控。
- **建议**：
  - 定期清理过期会话、过期/归档的幂等回执，回收已结束房间（对局中不可删）；
  - 优化 `roomsFor`：拆列（host、player uid 建索引）或限制扫描范围，避免每次 `json_each` 全表解析；
  - 可循环 SQLite `PRAGMA wal_checkpoint(TRUNCATE)` 或按文档在停服窗口做完整备份，控制 WAL 膨胀。

## 中优先级

### 3. 全库单写锁导致跨房间写操作全局串行

- **现象**：`server/store.js` 的事务使用 `BEGIN IMMEDIATE` + 单连接同步 SQLite，任意时刻全库只有一个写事务。
- **影响**：同一房间的并发提交实测最大约 6ms，可接受；但写入频率一旦达到每秒数百次（约数百桌同时操作），写队列排队直至触发 5s `busy_timeout` 后报 500。属于架构性上限，不是缺陷。
- **建议**：维持「低并发线下房间」的定位即可；若未来支撑更大规模同时在线，再考虑按房间分片、读写分离或迁移存储，不要在同一文件上多开写连接。

## 低优先级 / 备忘

### 4. 首页「我的牌桌」在房间积压时反复做全表扫描

- 每次进入首页与错误恢复时调用 `GET /api/me/rooms`，均会触发上面的全表 `json_each`。与第 2 条一并解决即可。

### 5. 事件循环内同步结算链的阻塞窗口

- 最后一份提交触发结算在**同一数据库事务内**同步完成（`server/engine.js` `settleIfComplete`），12 人骑士链式结算最坏会造成事件循环数十毫秒的阻塞。实测单次最坏在毫秒级、人眼不可感知，仅为后续在「更大房间/更长技能链」上的观察项，无需现在处理。

### 6. 监控与压测资产缺失

- 缺少面向真实 HTTP 栈的并发压测用例（本次多人场景结论来自临时脚本，未纳入 `tests/`）。建议补充一个可重复的并发轮询 + 并发提交压测用例，作为长期运营的回归基准。

---

**未发现问题的已排除项**：Node 单线程 + 同步 SQLite 在几十人规模无延迟问题；每用户限流 180 次/分钟、建房 8 次/分钟均高于正常用量；幂等回执 + 事务回滚保证并发重复提交不会重复结算。